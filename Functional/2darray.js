var readline=require('readline-sync');
var M=readline.question("Enter the number of rows");
var N=readline.question("enter the number of columns");

var mat=new Array();
let s=require('../utility')

console.log(matrix(M,N));